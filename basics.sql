CREATE TABLE students (
    Id int,
    name VARCHAR(100),
    city VARCHAR(50),
    Details VARCHAR(200)
);

CREATE TABLE cities (
    ID INT,
    Name VARCHAR(100),
    City VARCHAR(50),
    Details VARCHAR(200)
);